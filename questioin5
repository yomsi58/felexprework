#write a function to check to see if all numbers in thge list
#are consecutive  numbers .for examples,[2,3,4,5,6,7] are
# are consecutive numbers , but [1,2,4,5] are not consecutive 
# numbers the reurn should be boolean type. def
#is_consecutive(a_list):

def checkConsecutive(l):
    return sorted(l) == list(range(min(l), max(l)+1))
     
lst = [2,3,4,5,6,7]
print(checkConsecutive(lst))